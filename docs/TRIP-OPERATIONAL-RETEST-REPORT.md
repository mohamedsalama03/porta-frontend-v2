# PORTA DELIVERY

# NEXT.JS TRIP OPERATIONAL RETEST REPORT

Completed 13 September 2026. **TRIP OPERATIONAL RETEST PASSED.** The approved contract is synchronized, the preserved authenticated live checks pass, and all required regression gates pass.

## Backend Revision

User-approved target: `064f2903b7f612ff486428d00039387538a040e9`.

Frontend: `http://localhost:3000`. Configured API: `http://localhost:8080` through `NEXT_PUBLIC_API_BASE_URL`. Work remained inside the independent Next.js frontend. The running backend revision is user-supplied; no backend repository or database was inspected.

## OpenAPI

- Path: `contracts/porta-api-v1.openapi.json`.
- Verified SHA-256: `2b025d1d9c33407920869eea6a0cf5bf5cc6971643b28e6e0c36f54a682ae658`.
- The first `npm run api:check` after replacement detected stale generated metadata and exited 1.
- The existing `npm run api:generate` process completed successfully, producing 63 schemas and 51 operations.
- The subsequent `npm run api:check` passed, exit 0.
- `src/lib/api/generated.ts` changed only its SHA-256 comment on line 2. All other generated bytes, including validators, types, enum definitions and operation metadata, stayed identical. No generated schema was manually edited.
- The replacement changes only the attach/detach operation descriptions and one trailing newline. Restoring the old descriptions and newline in memory reproduced the exact previous SHA-256; this verifies that API routes, schemas, enums and all other contract content are unchanged. The updated descriptions agree with the preserved live observations.

The earlier passing check against SHA `90d5a0905238d2c9447e1002332fb65b63b1dc552c4c98b441627b9c809b6ae4` belongs to the paused phase and is not used as proof of current synchronization.

## Authentication and Evidence

The completed operational checks reused the authenticated **Porta Test Admin** session. No credentials or cookies were requested, exported or transferred. On resumption, the user's instruction to preserve successful live evidence was followed: no additional live operational writes were needed because contract review found no contradiction.

Evidence consists of the actual API-backed UI, full reloads, reopened forms and source inspection, recorded in `reports/trip-operational-retest/progress.json`. Raw authenticated request/response bodies, exact HTTP status codes and wire timestamps were not captured. Expected serialization is established by the unchanged source and focused tests, not represented as a network capture. Absent relationships were verified through fresh UI values and the documented null handling.

## New Trip Fixture

| Entity | Identifier / distinguishing information |
| --- | --- |
| New disposable trip | `01m2e07783fw899t87062pvnvd` |
| QA context | `PORTA-QA-20260913-1715` catalog references; the new trip has a distinct ID |
| Origin city | `01m2dw63kc994q8ysh8tgq16ac` — `PORTA-QA-20260913-1715-CITY-A` |
| Destination city | `01m2dw74z270ck5fxhnj9rdjm1` — `PORTA-QA-20260913-1715-CITY-B` |
| Trip driver | `01m2dwbywn3hzh7wpftdvg5h0k` — `PORTA-QA-20260913-1715-DRIVER` |
| Shipment | `01m2dwmhj3j3qgvga408yc7z58` |
| Tracking number | `PTA-260913-894B6DA768A453B6` |

The two existing QA cities were temporarily enabled for the new trip form, then restored to inactive; both inactive rows were verified. No other catalog records were created or changed in this focused retest. The new trip remains SCHEDULED with its driver and zero shipments.

Historical trip `01m2dwqsp1angqezd73gaagytt` was not modified or used as proof of the datetime fix. Its older shifted timestamps are outside this retest result.

## Trip Create Datetime

**TRIP CREATE DATETIME: PASS**

All times below are on **14 September 2026**, in **Africa/Tripoli**.

| Check | Departure | Arrival |
| --- | --- | --- |
| Entered form values | `10:00` | `12:00` |
| Expected explicit-offset serialization | `2026-09-14T10:00:00+02:00` | `2026-09-14T12:00:00+02:00` |
| Detail after successful creation | `10:00` | `12:00` |
| Detail after full browser reload | `10:00` | `12:00` |

**TRIP GET ROUND-TRIP: PASS.** No two-hour shift appeared.

## Trip Edit Datetime

**TRIP EDIT DATETIME: PASS**

| Check | Departure | Arrival |
| --- | --- | --- |
| Entered form values | `10:00` | `13:00` |
| Detail after save | `10:00` | `13:00` |
| Reopened editor | `2026-09-14T10:00` | `2026-09-14T13:00` |
| Detail after another full reload | `10:00` | `13:00` |

The Arabic detail displayed arrival as `01:00 م`, equivalent to 13:00. The unchanged departure was preserved while arrival was edited.

## Datetime Frontend Contract

**DATETIME FRONTEND CONTRACT: PASS**

`src/features/operations/write-model.ts` derives the explicit offset through `Intl.DateTimeFormat` with `Africa/Tripoli`. The edit adapter compares local form values with `localDateTimeValue` and sends changed fields. `src/lib/formatters/index.ts` parses absolute instants and formats them in `Africa/Tripoli`; the reopened editor converts the returned absolute instant through the same zone.

No subtract-two-hours adjustment, hardcoded UTC+2 conversion, offset removal or interpretation of backend UTC as local wall-clock time was introduced. Mutation validation and JSON transport do not add another timezone shift.

## Attachment Fixture

| Required condition | Observed before attachment |
| --- | --- |
| Trip status | SCHEDULED |
| Shipment status | RECEIVED |
| Existing membership | Trip count 0; shipment had no trip |
| Origin / destination | Exact same QA city IDs on trip and shipment |
| Branch | Both null; trip editor confirmed an empty branch |
| Driver | Trip and shipment already referenced the same active QA driver |

This fixture satisfies the approved description. The old failure used a null shipment branch and a non-null trip branch; that invalid combination was not repeated. The shipment was never advanced to PREPARING to enable attachment.

## Attach

**TRIP SHIPMENT ATTACH: PASS**

One submission through the existing review/confirmation UI succeeded. The trip displayed a count of **1**, exactly one shipment row, and successful attachment feedback. The shipment detail showed the new trip ID and the matching QA driver. Reopening the trip still showed one member. No conflict or duplicate membership was observed.

Driver alignment was confirmed, but both driver values already matched before attachment. A divergent-to-aligned driver synchronization case was not exercised or claimed.

## RECEIVED Attachment

**RECEIVED ATTACHMENT: PASS**

The shipment remained RECEIVED during successful attachment. No frontend PREPARING requirement or lifecycle workaround was introduced.

## Detach

**TRIP SHIPMENT DETACH: PASS**

One confirmed removal through the documented UI succeeded. Membership became zero. A freshly opened shipment page showed both trip and assigned driver absent (`غير متاح`), while status remained RECEIVED. The trip's own QA driver was unchanged.

The optional live branch-mismatch conflict check was not repeated. Existing focused regression coverage checks the neutral conflict feedback; no unnecessary invalid fixture was created.

## Final Membership State

| Record | Final observed state |
| --- | --- |
| New trip | SCHEDULED; **0 members**; no shipment row; driver `01m2dwbywn3hzh7wpftdvg5h0k`; branch null; schedule `10:00` / `13:00` Libya time |
| Disposable shipment | RECEIVED; `trip_id:null`; `assigned_driver_id:null`; branch null |
| QA cities | Both restored to inactive |
| Historical trip | Untouched |

## Focused Tests

**PASS — 15 tests in 3 files, exit 0.**

Command: `npx vitest run tests/unit/operation-writes.test.tsx tests/unit/idempotency.test.ts tests/unit/formatters.test.ts`.

Coverage includes trip offset conversion and unchanged edit payloads, attachment validation, attach/detach request shape, confirmation/retry behavior, neutral conflict feedback, shared idempotency and formatting. Actual create/edit/reload and backend relationship outcomes are established by the preserved live evidence; these unit tests alone do not prove those outcomes.

The process-local `VITEST_MAX_WORKERS=1` setting was restored after execution. No test assertion, timeout or configuration was changed. Log: `reports/trip-operational-retest/logs/01-focused.log`.

## Regression Gates

| Required command | Result |
| --- | --- |
| `npm run api:check` | PASS, exit 0, synchronized approved contract |
| `npm run lint` | PASS, exit 0 |
| `npm run typecheck` | PASS, exit 0 |
| `npm run test` | PASS, exit 0; **142/142 tests in 22/22 files**, first attempt with process-local `VITEST_MAX_WORKERS=1` |
| `npm run test:e2e` | PASS, exit 0; **16/16 Playwright tests** |
| `npm run test:live` | PASS, exit 0; **3/3 live Playwright tests** |
| `npm run build` | PASS, exit 0; production compile and TypeScript checks succeeded, **8/8 static pages** generated; prebuild contract check also passed |

The focused 15 tests are a subset of the 142-test full suite and are not added to its total. The full unit suite kept its existing assertions, timeouts and configuration; the worker environment override was restored after execution.

The regular Playwright suite exercises the repository's isolated foundation test app. Live Playwright covers actual public catalogs/schema/CORS, unauthenticated session discovery, CSRF/session-cookie behavior, login accessibility and safe redirects. Neither automated suite is presented as authenticated trip-write coverage; that evidence comes from the preserved staff-session checks above.

All post-synchronization checks passed on their first attempt; no assertion, timeout, test or application change was needed. The initial stale generated hash was the expected contract-sync failure, resolved through generation before these gates. Exact commands, counts, exits and logs are recorded in `reports/trip-operational-retest/regression-results.json` and `reports/trip-operational-retest/logs/01-focused.log` through `08-build.log`.

## Frontend Source Changes

**Application logic changes: ZERO.** No demonstrated frontend defect required a repair.

| File / category | Exact change |
| --- | --- |
| `contracts/porta-api-v1.openapi.json` | User-supplied replacement: two attach/detach operation descriptions and one trailing newline; no route/schema/enum changes |
| `src/lib/api/generated.ts:2` | Generated SHA-256 comment updated by `npm run api:generate`; all other bytes identical |
| `docs/TRIP-OPERATIONAL-RETEST-REPORT.md` | New focused acceptance report |
| `reports/trip-operational-retest/` | Evidence updates, command logs, preserved pre-sync artifact and paused progress snapshot, final progress/results/source comparison; excluded from application source comparison |

Compared with the retained 171-file baseline: **2 existing files changed**, **1 report added**, **0 missing files**, **0 application-logic changes**. No tests, configuration, other documentation or runtime logic changed. Exact before/after hashes are recorded in `reports/trip-operational-retest/source-comparison.json`.

## Remaining Documented Gaps

These existing limits are carried forward without repeating the broader acceptance campaign:

- Attachment selection still accepts manual shipment IDs; server-backed selection remains deferred.
- Allowed lifecycle action discovery remains unavailable and unimplemented.
- Successful same-key replay, different-payload same-key conflict and exact key bytes need a suitable harness. This retest does not establish those behaviors.
- Safe payment-write fixtures and a safe live 429 trigger remain unavailable; no payment or rate-limit exercise was performed.
- Users/audit access remains outside the current account's authorization.
- Authenticated mobile/tablet resizing, automated axe and Lighthouse remain subject to the previously documented environment limits.
- Raw authenticated wire evidence was unavailable. The initial matching-driver fixture does not prove synchronization from a different driver.
- Frontend Git metadata is unavailable; source integrity uses the retained SHA-256 baseline. Existing default-worker unit timing sensitivity is handled with the previously established process-local worker limit, without changing tests.

This report supersedes only the prior F1/F3 trip findings in `docs/ADMIN-DASHBOARD-OPERATIONAL-ACCEPTANCE-REPORT.md`; unrelated historical results are not reopened. No public Order Form, public Tracking, Driver mobile, GPS, new feature, lifecycle discovery or deployment was started.

## F1 Frontend Verification

**F1 FRONTEND RETEST: PASS**

The new trip's create, edit, reopened editor and reload observations preserve the intended Libya-local times.

## F3 Frontend Verification

**F3 FRONTEND RETEST: PASS**

The valid branch-matched RECEIVED fixture attached once, displayed one membership, and detached with the required final relationships and unchanged status/trip driver.

## Final Result

**TRIP OPERATIONAL RETEST PASSED**

F1 and F3 pass for the new disposable trip and branch-matched shipment. The synchronized contract and all required frontend gates agree with the preserved successful live observations. No new operational defect was found within this focused scope.
